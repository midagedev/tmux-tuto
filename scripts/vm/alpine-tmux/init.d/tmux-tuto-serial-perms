#!/sbin/openrc-run

description="Set ttyS1 permissions for tmux-tuto probe channel"

depend() {
  need devfs mdev
  before agetty
}

start() {
  if [ -e /dev/ttyS1 ]; then
    chgrp tuto /dev/ttyS1 || true
    chmod 0666 /dev/ttyS1 || true
  fi
}
